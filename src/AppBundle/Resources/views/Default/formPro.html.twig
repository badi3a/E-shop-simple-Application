{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig'%}
{% block body %}
    <br><br>
    {% for flashMessage in app.session.flashbag.get('prod_created') %}
        <div class="alert alert-info">
            {{ flashMessage }}
        </div>
    {% endfor %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Ajouter" class="btn btn-primary">
    {{ form_end(form) }}

{% endblock %}
{% block title %}

{% endblock %}

